# Marketing site (`next dev` :3000). Staff CRM runs separately on :8080 — see NEXT_PUBLIC_CRM_URL.

NEXT_PUBLIC_CRM_URL=http://localhost:8080
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000/leads
API_INTERNAL_URL=http://localhost:4000/api

# Marketing /call page — click-to-call (E.164). Change to your real business line.
NEXT_PUBLIC_DIRECT_CALL_PHONE=+18005550100
